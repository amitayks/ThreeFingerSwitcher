> Wave 3. Consumes `AgentConversation`/`AgentSessionID`/`AgentMessage` (`ai-conversation-runtime`) and `ToolStepResult`/`ToolStepStatus` (`ai-tool-routing`) verbatim; provides `ParkScheduler` for `ai-batched-runtime-and-context` to fill, and receives `escalate` from `ai-background-autonomy`. §1–§5 are pure MLX-free Core (`swift test`); §6–§8 are overlays/wiring (`xcodebuild` compile-verify + user run-verify); §9 verifies.

## 1. Park types (pure Core)

- [x] 1.1 Add `ParkState` (`active`/`parked`/`needsYou`/`idle`) and `ParkedSession` (`id: AgentSessionID`, `title`, `state`, `badgeCount`, `nextRunAt`, `updatedAt`) in `AI/Parked/ParkedSession.swift`, verbatim from blueprint 3.5. Consume `AgentSessionID` from `ai-conversation-runtime` — do NOT redefine it. *(verify: `swift test` — Codable round-trip; `id` identity is stable across encode/decode.)*
- [x] 1.2 Add `ParkError: Error, Equatable, LocalizedError` (`.storeUnavailable`/`.persistFailed`/`.resumeMissing`) with a clean per-case `errorDescription`; route it through `AIError.message(for:)` (extend the translator). *(verify: `swift test` — each case yields a clean headline, no raw OS text; translator returns an `AIPresentedError`.)*

## 2. Scheduler (pure Core — the batching seam)

- [x] 2.1 Add `protocol ParkScheduler: Sendable` (`runnableSessions(now:maxSlots:) -> [AgentSessionID]`, `didAdvance(_:result: ToolStepResult)`, `escalate(_:reason:)`) in `AI/Parked/ParkScheduler.swift`. Consume `ToolStepResult`/`ToolStepStatus` from `ai-tool-routing`. *(verify: compiles under `swift build`.)*
- [x] 2.2 Implement `SerialParkScheduler`: `runnableSessions` returns **at most one** session regardless of `maxSlots`, filtered to `state == .parked` and (`nextRunAt == nil || nextRunAt <= now`), `needsYou` **excluded**, ordered by `nextRunAt` then `updatedAt` (oldest-waiting first); `didAdvance` settles the run and updates the card (`.done` → bump `updatedAt` + `badgeCount`; `.failed(headline:)` → failed badge with the clean headline; `.awaitingApproval` for a dangerous step → `escalate`). *(verify: `swift test` — one-active-now; `needsYou`/future-`nextRunAt` excluded; priority order; `didAdvance` state transitions.)*
- [x] 2.3 Test the **K-ready** contract with a stub `ConcurrentParkScheduler` that returns up to `maxSlots`: asserts the protocol needs **no change** to serve K slots (the drop-in). *(verify: `swift test` — `maxSlots: K` returns ≤K; `maxSlots: 1` returns ≤1; same protocol.)*

## 3. Durable store (Core, IO bridged off-main)

- [x] 3.1 Add `protocol ParkedSessionStore: Sendable` (`all() -> [ParkedSession]`, `conversation(_:) -> AgentConversation?`, `upsert(_:)`, `remove(_:)`, `oneLineResume(_:) -> String?`) in `AI/Parked/ParkedSessionStore.swift`. Own persistence; consume `AgentConversation` from `ai-conversation-runtime` — do NOT duplicate the type. *(verify: compiles under `swift build`.)*
- [x] 3.2 Implement the durable store (JSON under app-support, IO serialized off-main per the Files-band sync-model + async-cache pattern); map `FileManager`/coding failures to `ParkError` **at the boundary**. *(verify: `swift test` — in-memory/temp-dir round-trip: upsert→all→conversation→remove; resume read; a forced IO failure surfaces `ParkError`, never throws raw / never silent.)*

## 4. Lifecycle (pure Core)

- [x] 4.1 Add `ParkLifecycle` (`maxParked`, `idleTimeout`, `sleepable(_:now:) -> [AgentSessionID]`, `evictable(_:now:) -> AgentSessionID?`) in `AI/Parked/ParkLifecycle.swift`, with injected `now:`. Eviction picks the least-recently-updated `idle` session; **never** `active`/`needsYou`/`thinking`. *(verify: `swift test` — eviction victim; over-cap with all-non-idle yields nil; sleepable respects timeout; active/needsYou never chosen.)*
- [x] 4.2 Add `protocol ParkSleepRequesting: Sendable` (the seam for the batched slice to drop the KV cache) + the summarize-and-sleep flow that persists a **one-line resume** to the store and requests the drop. Keep Core MLX-free — the KV drop is the batched slice's; this slice owns *when* + the persisted resume. *(verify: `swift test` — a sleepable session persists a resume and calls the (stub) sleep seam; a non-idle session does not.)* (Implemented as `ParkLifecycleCoordinator.runSleepPass` over the pure `ParkLifecycle`.)
- [x] 4.3 Add the **discard** flow: cancel pending generation via `Task` cancellation (a cancellation is NOT a failure — no `failed` badge), `remove` from the store, drop the rail card; document that **completed side effects are not rolled back**. *(verify: `swift test` — discard removes the session and leaves no `failed` state; a cancellation does not transition to `failed`.)* (`ParkLifecycleCoordinator.discard`.)

## 5. Overscroll-park decision + anchor/reveal models (pure Core)

- [x] 5.1 Add `OverscrollPark.shouldPark(dy:canvasAtBottom:overscrollThreshold:)` (Core): parks only when `canvasAtBottom` and the UP excursion exceeds `overscrollThreshold` (a value above `canvasResolveThreshold`). *(verify: `swift test` — at-bottom + over-threshold parks; below-threshold scrolls; not-at-bottom never parks; symmetric with the `canvasAtTop` guard.)*
- [x] 5.2 Add `NotchHomeZoneAnchor.zoneRect(size:visibleFrame:safeAreaTop:)` + `railRect(zone:size:visibleFrame:)` (Core, pure geometry): notch tuck when `safeAreaTop > 0`, top-center menu-bar tab when `== 0`; the rail hangs below the zone, shift-only clamped on-screen (the `DockHoverModel.clamp` idiom). *(verify: `swift test` — notch vs tab anchor; rail clamps within `visibleFrame`; never resizes to clamp.)*
- [x] 5.3 Add `NotchRevealModel.feed(cursor:zoneRect:railFrame:now:) -> Decision` (Core), mirroring `DockHoverModel`: a unified zone+rail live area, grace-dismiss, `now:` injected. *(verify: `swift test` — reveal when in the zone; keep when traveling zone→rail; grace-dismiss after leaving; deterministic via injected `now:`.)*
- [x] 5.4 Add `AppSettings` persisted keys: `agentMaxParkedSessions`, `agentParkIdleTimeout`, `agentOverscrollParkThreshold` (camelCase, agent-scoped; default-preserving; included in `resetToDefaults`). *(verify: `swift test` — defaults present; reset restores them.)*

## 6. Notch home-zone overlay (app/GemmaRuntime — compile-verify)

- [x] 6.1 Add `Overlay/NotchHomeZoneOverlay.swift`: a `NotchHomeZoneOverlayController` on the `DockPreviewOverlayController` recipe — `[.borderless, .nonactivatingPanel]`, `ignoresMouseEvents = false`, `acceptsMouseMovedEvents = true`, `level = .popUpMenu`, `.canJoinAllSpaces`, never key/main, `show(at:)`/`move(to:)`/`hide()` with **synchronous `orderOut`**. *(verify: `xcodebuild` compile; user run-verify the non-activating, no-focus-steal panel.)*
- [x] 6.2 Add the rail view: a horizontally-**scrollable** `HStack` of parked cards, each card budding in via `bubbleMorph`; per-card **badge** off `ParkState`/`badgeCount` (thinking / done-count / needs-you accent / failed-with-clean-headline). *(verify: `xcodebuild` compile; user run-verify the badges + scroll.)*
- [x] 6.3 Add the **ambient needs-you notch glow** on the resting zone via `BreathingGlowBackdrop`/`PulseHalo` (reuse `WizardMotion`) — soft slow pulse, present **iff** any session is `needsYou`, persists until every `needsYou` is addressed; never modal, never re-fronts the panel. *(verify: `xcodebuild` compile; user run-verify the glow is peripheral and clears only on addressing.)*

## 7. Rail controller + overscroll-park + fly-up wiring (app — compile-verify)

- [x] 7.1 Add `NotchHomeZoneController` (app): wire `GlobalCursorMonitor` (passive, no Input Monitoring) → edge-gate near the top-center zone → `NotchRevealModel` → `overlay.show/move/hide`; coarse re-feed timer only while shown; **synchronous** teardown. Gated by the agent-feature opt-in. *(verify: `xcodebuild` compile; user run-verify reveal/keep/grace-dismiss + no ghost on Space switch.)*
- [x] 7.2 Wire **pull-back-to-active** (controller side): clicking a card invokes `onRestore(AgentSessionID)` → `dismiss()` + `refresh()` (clears the glow when the last `needsYou` is addressed). The `state → .active` / `badgeCount → 0` mutation + the re-seed from the stored `AgentConversation` is handed to the consumer via `onRestore`. *(verify: `xcodebuild` compile.)* NOTE: the `onRestore` entry point is now fully wired through `ParkController` — clicking a card → `NotchHomeZoneController.onRestore` → `ParkController.restore(id)` which mutates the rail row (state → `.active`, `badgeCount → 0`), republishes (clearing the glow when the last needs-you is addressed), and hands the stored `AgentConversation` to `onRestoreConversation` (the `AppCoordinator` seam for `ai-conversational-canvas`'s canvas re-seed). The canvas-side re-seed render is the user run-verify in a stable-signed build (9.3).
- [x] 7.3 Wire **overscroll-park** at the `AppCoordinator` seam: read the recognizer's raw `launcherCanvasResolve(dx:dy:)`, consult `OverscrollPark.shouldPark` with the canvas's `canvasAtBottom`, and on park fly the canvas UP into the zone via `BubbleMorph` (recede side) + upsert the `ParkedSession`. Recognizer unchanged. *(verify: `xcodebuild` compile; user run-verify the fly-up parks, and a normal scroll-to-bottom does not.)* DONE: `AICommandExecutor.canvasAtBottom`/`onPark`/`parkHandoff()` and `AICommandCanvasView` landed (sibling `ai-conversational-canvas`), so the seam is fully wired: `AppCoordinator.launcherCanvasResolve` accumulates a pure-UP-at-bottom excursion (the `canvasDiscardAccum` step-accumulation precedent, recognizer UNTOUCHED) and consults `OverscrollPark.shouldPark(dy:canvasAtBottom:overscrollThreshold:)` with `agentOverscrollParkThreshold`; over-threshold → `LauncherOverlayController.parkCanvas()` → `executor.parkHandoff()` (hands the live `AgentConversation` to `ParkController.park` → durable upsert + scheduler row + eviction pass) then the overlay recedes (synchronous teardown). The fly-up morph (BubbleMorph recede) + the live "normal scroll-to-bottom does not park" feel are the user run-verify in a stable-signed build (9.3).

## 8. Integration with the batched runtime + escalation (compile-verify)

- [x] 8.1 Expose the `ParkScheduler` instance to the batched runtime (`ai-batched-runtime-and-context`) so it calls `runnableSessions(now:maxSlots:)` to fill its remaining slots and reports back via `didAdvance`; the foreground active session keeps slot 0. *(verify: `xcodebuild` compile; cross-slice contract check against the batched slice's `BatchedLLMRuntime`.)* DONE (this slice's side): `ParkController.parkScheduler` exposes the `SerialParkScheduler` as a `ParkScheduler` and `ParkController.didAdvance(_:result:)` forwards a batch step result (republishing the rail); the K-ready contract is unit-tested (`maxSlots: 1` and `maxSlots: 8` both ≤1, same protocol). The batched runtime (`BatchedGemmaMLXRuntime`, MLX) correctly does NOT reach into Core's scheduler — Core stays MLX-free; the app injects/forwards at the seam. The live batched fill of K slots is the batched slice's to drive through this unchanged protocol.
- [x] 8.2 Accept `ParkScheduler.escalate(_:reason:)` calls from `ai-background-autonomy` (dangerous write while parked) → set `state = .needsYou` + badge + light the ambient glow. *(verify: `xcodebuild` compile; user run-verify an escalated parked session lights the glow without a modal.)* `SerialParkScheduler.escalate` sets `.needsYou` + badge (tested); `NotchHomeZoneController.refresh()` lights the glow iff any session is `.needsYou`. NOTE: the *caller* (`ai-background-autonomy`) is not yet on disk; the entry point is in place.

## 9. Verify

- [x] 9.1 `swift build` + `swift test` green: scheduler (one-active-now + K-ready), store round-trip + `ParkError` mapping, lifecycle (evict/sleepable/discard), overscroll-park, anchor (notch vs tab) + reveal model, `AIError` routing. The full `ThreeFingerSwitcher` product builds + links (Core + GemmaRuntime/MLX) via `xcodebuild` compile-verify (no app build/sign/install by the agent). *(swift test: 1169 tests, 0 failures; `xcodebuild … build CODE_SIGNING_ALLOWED=NO` → BUILD SUCCEEDED.)*
- [x] 9.2 `openspec validate --strict` passes; the `ai-parked-sessions` spec (ADDED-only, new capability) matches the implementation; consumed types (`AgentConversation`/`AgentSessionID`/`ToolStepResult`) match the sibling slices' committed definitions verbatim. (`BatchedLLMRuntime` is a sibling slice not yet on disk; the `ParkScheduler` seam it consumes is in place.)
- [ ] 9.3 **User run-verify** in a stable-signed build: overscroll-past-bottom flies the canvas into the notch; on a notched display the zone hugs the notch, on an external display it is a top-center tab; cursor-to-notch reveals the scrollable rail with correct badges; a needs-you escalation lights a peripheral glow (no modal) that clears only on addressing; pulling a card back restores the same conversation; discard cancels pending work but leaves completed side effects done; an idle session summarizes-and-sleeps and restores from its one-line resume. *(agent cannot run a stable-signed build / drive live cursor + notch + Space-switch behavior — left for the user.)* **SUPERSEDED by `notch-native-conversations`:** the overscroll-park and pull-back-restore halves of this verify are removed by the pivot (conversations become notch-native; the canvas never parks); the rail/badges/glow/discard/sleep halves are re-verified by that change's own run-verify task.

## 10. Notch-attached refactor — the panel is an extension of the notch (NotchNook look)

Refines §5/§6 and the two notch requirements: the home zone stopped "floating a little below the notch" and now **attaches to / merges with** the notch on a notched display, while the tab degradation is preserved for notchless/external. Hover-reveal is unchanged.

- [x] 10.1 `NotchHomeZoneAnchor.notchRect(screenFrame:safeAreaTop:auxLeft:auxRight:)` — runtime detection of the cutout box (gap between the two menu-bar areas flanking the camera housing; nil ⇒ tab path). *(swift test.)*
- [x] 10.2 Attached-mode pure geometry: `attachedNubRect` (nub flush at the notch bottom, centered), `attachedPanelRect` (top at the physical top so the black merges with the notch; width floored at `notch.width + 2·minNotchFlank`; total height = notch band + content), `attachedLiveZone` (unions nub + panel + notch band so moving up into the notch docks). Existing tab-mode `zoneRect`/`railRect`/`liveZoneRect` untouched. *(swift test — 5 new attached-mode cases; all prior anchor/reveal tests still green.)*
- [x] 10.3 `Overlay/NotchShape.swift` — a custom `Shape` carving a centered tongue flanked by two concave fillets so the software surface reads as one shape with the hardware notch; opaque black fill under the band; body corners rounded; radii clamped for small panels. *(swift build + a bounded/carves-top path unit test.)*
- [x] 10.4 `NotchHomeZoneOverlay.swift` — `NotchAttachment` (`.notch(cutout:)` / `.tab`) on the view-model; the rail view branches its chrome (`NotchShape` + opaque fill + notch-band top inset for `.notch`; `RoundedRectangle` + `.regularMaterial` for `.tab`). *(swift build.)*
- [x] 10.5 `SwitcherPanel.reachesPhysicalTop` (opt-in, default off) overrides `constrainFrameRect` so the notch panel can reach the physical top over the menu bar; set on the notch panel only. *(swift build.)*
- [x] 10.6 `NotchHomeZoneController` — read `NSScreen.auxiliaryTopLeftArea/RightArea` + safe-area, resolve the notch box, branch zone/rail/live-zone + set the view-model attachment before show; `.tab` fallback when the notch box is unresolvable (menu-bar hidden / notchless / external). *(swift build.)*
- [x] 10.7 **Merge black-on-black, no carve.** Removed `NotchShape` entirely: the attached chrome is a plain opaque-**black** rounded rectangle (`UnevenRoundedRectangle` — flat top so it grows from the physical top, rounded bottom) whose black simply spans **behind** the (also black) notch, so they read as one shape with no cutout to align and no dependence on the notch's corner radius. *(swift build; the `NotchShape` unit test was removed with the file.)*
- [x] 10.8 **Smooth ease-in-out spread.** Replaced the spring bubble-morph on the notch container with a `@Published isExpanded` → `scaleEffect(anchor: .top)` + opacity spread, flipped inside `withAnimation(.easeInOut)` by the controller: grows out of the notch on reveal, recedes back in on the grace-dismiss (`reveal(at:)` / `hide(animated:)` with a cancellable deferred `orderOut`; restore/feature-off stay synchronous; the re-feed timer pauses while `isReceding`). *(swift build + swift test green.)*
- [x] 10.9 **Center the content.** The cards were left-hugging (the panel is floored wider than a few cards) and sat low (top-only notch inset). Now centered on both axes via a `GeometryReader` + `frame(minWidth: geo.width, minHeight: geo.height, alignment: .center)` — horizontal centering that still scrolls on overflow, vertical centering that keeps the row clear of the notch (the panel carries a notch-band of headroom). *(swift build.)*
- [x] 10.11 **No top border.** Two layers: (a) the SwiftUI chrome hairline now traces only the sides + rounded bottom (an open `NotchPanelBorderShape`), omitting the top edge; (b) the **window itself** (the outer container) casts **no shadow** in attached mode (`hasShadow = false` + `invalidateShadow`), since a window drop-shadow at the top read as a border where the panel merges into the notch/menu bar. Tab mode keeps its shadow. *(swift build.)*
- [ ] 10.10 **User run-verify** on a real notched Mac: the revealed panel merges seamlessly with the notch (black-on-black, no gap, no seam, grows from behind the notch to full width/height), the cards are centered in the dock, it spreads open/closed smoothly (ease-in-out) out of and back into the notch, the panel draws over the menu bar while expanded, moving the cursor up into the notch docks, and an external/notchless display still shows the top-center tab. *(agent cannot see the notch or run a stable-signed build.)* **CARRIED FORWARD by `notch-native-conversations`:** the merged-notch look is unchanged by the pivot; its run-verify task covers this checklist on the new expanded-conversation surface.
