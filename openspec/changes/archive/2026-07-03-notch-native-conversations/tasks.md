# Tasks — notch-native-conversations

Ordering: extract the engine FIRST (so nothing is ever deleted before its new home exists), then revert the executor/canvas/coordinator, then build the notch surface on the engine, then re-home tests. Every Core step verifies under `swift build` + `swift test`; overlay steps are `xcodebuild` compile-verify. Surgical edits only — the tree carries a concurrent wave's uncommitted work in `AICommandExecutor.swift` / `AICommandCanvasView.swift` / `ParkController.swift`.

## 1. Extract the engine (Core — design D1)

- [x] 1.1 Create `AI/Parked/NotchSessionEngine.swift`: `@MainActor ObservableObject` with the conversational state subset (`.idle`, `.unavailable`, `.loadingModel`, `.conversing(partial)`, `.awaitingTurn`, `.awaitingApproval(TaskReview)`, `.failed(message)`), `@Published thinking`, `@Published toolSteps`, `@Published pendingAttachments` (API only), and the bound `conversation: AgentConversation?`. Deps injected as in the executor's conversational half: `ModelManager`, `TaskDispatching`, tool registry + candidate source + `skillTools`, optional `BackgroundToolRunner`, `reasoning` closure.
- [x] 1.2 Move the turn machinery from `AICommandExecutor` wholesale: `runTurn` (ChatTemplate build, thinking/response channels, cancellation-is-not-failure, route-loop integration), `send(_:images:)`, `continueConversation`, `makeApprovalGate`/`approve()`/`skip()`/`appendToolStep`, attachment seams (`attachScreenshot`/`attachClipboardImage`/remove/clear as they exist today). Drop in the move: `SeedKind`/`BareSeedDefault` folding, `conversationOutput`/`extractLatest`, `canvasAtTop`/`canvasAtBottom`, park/restore transitions.
- [x] 1.3 Engine session verbs for the controller seam: `bind(_ conversation:)` (→ `.awaitingTurn`, or `.awaitingApproval` context re-derived by the next escalated step), `unbind(persist:)` (returns the current `AgentConversation` snapshot WITHOUT cancelling an in-flight turn — the turn's completion writes through via a `onTurnPersisted` callback), `startNew(title:)` (fresh `AgentConversation`, `.awaitingTurn`), and `onTaskComplete: (AgentSessionID) -> Void` (terminal completion → controller marks the row completed).
- [x] 1.4 Delete `AI/Agent/BareSeedDefault.swift` and `AI/Parked/OverscrollPark.swift` (behavior removed by the spec delta; nothing else references them once 2.x lands). `swift build` green.

## 2. Revert the executor + canvas + coordinator to v1 one-shot (design D2)

- [x] 2.1 `AICommandExecutor`: remove `fireConversational`/`openConversational`/`isConversationalAsk`, `openConversationCanvas`/`startConversation`/`send`/`continueConversation`, `extractLatest`/`markCommitted`, `parkHandoff`/`restoreConversation`, the approval-gate seams, the attachments model, `conversation`/`seedKind`/`conversationOutput`/`pendingSeed*`/`session*` storage, `toolSteps`, `canvasAtBottom`, `onPark`/`onTaskComplete`; State sheds `.conversing`/`.awaitingTurn`/`.awaitingSeed`/`.parked`/`.awaitingApproval` (v1 cases + `isCommittable` stay; `canvasAtTop` stays for the at-top commit gate).
- [x] 2.2 `AppCoordinator`: route both fire call sites back to `fire(_:)` (the item path and the screen-region path); delete the park/restore wiring (`onPark`, `onParkCanvas`, `onTaskComplete`, `onRestoreConversation` + `restoreCanvas` call), the approval-compass branches in `launcherCanvasResolve` (DOWN-approve/RIGHT-skip, `extractLatest` vs `commit` fork → plain `commit()`), the overscroll-park read, and the composer attach-screenshot wiring (`onAttachCanvasScreenshot`).
- [x] 2.3 `AICommandCanvasView`: remove the thread renderer, composer + float-up placeholder + `@FocusState`, attachment chips, approval-card rendering, and every `executor.conversation` branch — back to the pure one-shot preview (streaming text + thinking disclosure + ready/commit hints + `.reviewingAction` armed-confirm + availability/no-input/failed states). `LauncherOverlayController`: drop `restoreCanvas`/`onParkCanvas` and the field-focus key/main flip; keep click-interactivity for the language/model pills.
- [x] 2.4 `swift build` + full `ThreeFingerSwitcherCore` compile green with the executor/canvas revert (tests re-homed in §5, may be red until then — build must be green).

## 3. Notch surface: session birth, expand-in-place, collapse (design D3/D4/D5)

- [x] 3.1 `ParkController` → session verbs: `newSession()` (durable-at-birth: fresh `AgentConversation` upserted + scheduler seeded + expand), `expand(id:)` (load conversation, bind engine, clear badge, clear glow-if-last via existing republish), `collapse()` (unbind engine persisting the snapshot, republish), `discard(_:)` unchanged as the single removal path; delete `park(_ conversation:)`'s canvas-handoff role (keep the internal upsert helper) and `onRestoreConversation`. Engine `onTaskComplete` → existing `didAdvance(taskComplete: true)` terminal flow. `ParkController` owns the engine instance (injected deps from `AppCoordinator`).
- [x] 3.2 `NotchHomeZoneViewModel.mode: .rail | .expanded(AgentSessionID)` + `NotchHomeZoneLayout.solveExpanded(visibleFrame:)` (wider/taller clamps, content scrolls); controller `railRectFor` branches on mode. Rail view gains the persistent "+ New chat" card (present at zero sessions).
- [x] 3.3 Expanded conversation view in `NotchHomeZoneOverlay.swift`: thread (user/assistant turns, collapsible thinking, live streaming partial), tool-step list, approval card with Approve/Skip buttons, failed card (clean headline + opt-in details + Retry), Copy button on assistant turns, composer (Enter sends), collapse chevron + Esc + notch-nub-click collapse paths, delete affordance in the header. Same merged-notch chrome (no second panel).
- [x] 3.4 Key-flip: `SwitcherPanel.keyInteractive = true` + `makeKey()` while the composer is focused; drop key BEFORE any `orderOut` on every collapse/teardown path (D5 ordering); never main, never `activate`. Feature-off/Space-switch teardown stays synchronous.
- [x] 3.5 Reveal pinning: while `.expanded`, `NotchHomeZoneController.handleCursor` short-circuits dismiss decisions (reveal model untouched); the re-feed timer keeps advancing relayout; collapse re-enters normal rail grace behavior. Expanding another card collapses the current one first.
- [x] 3.6 `xcodebuild` compile-verify the app target (overlay + controller + coordinator wiring). *(xcodebuild -scheme ThreeFingerSwitcher build CODE_SIGNING_ALLOWED=NO → BUILD SUCCEEDED.)*

## 4. Spec-pinned firewall checks (delta scenarios as code-level asserts)

- [x] 4.1 Verify by construction + test: a one-shot `fire` never touches the store/scheduler (no `AgentConversation` exists on the executor at all); the notch dock's rows come only from `newSession` / relaunch load.
- [x] 4.2 Expiry/deletion intact end-to-end on the new verbs: terminal completion auto-dismisses; idle-past-countdown auto-dismisses non-protected rows; discard from card menu AND expanded header cancels + removes; completed side effects never rolled back (existing lifecycle tests keep passing).

## 5. Re-home the tests (Core)

- [x] 5.1 `ConversationSessionTests` → drive `NotchSessionEngine` (startNew/send/continueConversation/thinking-never-re-fed/per-turn error/mid-turn discard); delete seed-acquisition cases (no seed at the notch).
- [x] 5.2 `ConversationalCanvasTests` → the canvas-machine cases die with the behavior (seed-wait, float-up state, extract-latest, bare-seed default, overscroll-park decision); keep/port anything exercising the engine (approval gate, tool steps) onto the engine; rename or fold the file as fits.
- [x] 5.3 `AICommandExecutorTests` → drop conversational halves; assert the v1 surface incl. the new "fire creates no conversation/session" invariant. `BatchedRuntimeTests` → re-target engine-driven conversations where they used the executor.
- [x] 5.4 `ParkedSessionsTests` → replace overscroll-park/restore cases with: new-session-durable-at-birth, expand-binds + badge/glow clear, collapse-persists-without-cancel (in-flight turn completes → badge), expand-other-collapses-current, just-born-session-survives-relaunch (store round-trip), rail-includes-new-chat-card model state.
- [x] 5.5 Full `swift test` green.

## 6. Verify

- [x] 6.1 `swift build` + `swift test` green (engine, executor v1 surface, park/session verbs, lifecycle, store); `xcodebuild` compile-verify of the full app target (Core + GemmaRuntime/MLX link) — no app build/sign/install by the agent.
- [x] 6.2 `openspec validate --strict` passes; deltas match implementation; consumed types (`AgentConversation`/`AgentSessionID`/`ToolStepResult`/`TaskReview`) untouched. *(swift test: 1466 tests, 0 failures; openspec validate --strict: valid.)*
- [ ] 6.3 **User run-verify** in a stable-signed build: an AI item auto-fires one-shot (DOWN at top applies, RIGHT discards, no composer anywhere on the canvas); nothing a quick action does ever produces a notch card; cursor-to-notch reveals the rail with "+ New chat" (also on an empty dock); new chat expands in place, typing works (panel key only while the field is focused; front app keeps focus otherwise and regains it on collapse), Enter sends and the reply streams; Approve/Skip resolve an escalated step and the glow clears; collapse mid-turn backgrounds the turn and the badge updates; delete works from card and header; an idle session expires after the countdown; relaunch rebuilds cards including a just-born session; merged-notch look + external-display tab unchanged (carries forward ai-parked-sessions 10.10).
