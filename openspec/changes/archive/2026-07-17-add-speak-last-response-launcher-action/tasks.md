# Tasks: add-speak-last-response-launcher-action

## 1. Implementation

- [x] 1.1 `SystemAction.speakLastResponse` + meta (System category, speaker symbol)
- [x] 1.2 `LaunchService.onSpeakLastResponse` closure + dispatch in `perform`
- [x] 1.3 Coordinator wiring to `speakLastResponse()`
- [x] 1.4 `swift build` + `swift test` green; archive with spec sync
